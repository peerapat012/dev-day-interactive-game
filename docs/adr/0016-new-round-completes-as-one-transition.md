# Complete a new round as one lifecycle transition

A new round becomes visible only after the prior summary is archived, active entries are cleared, and guest submission allowances are reset. If any step fails, the old active summary, entries, and guest allowances remain in place so the host can retry without exposing a partial round.
