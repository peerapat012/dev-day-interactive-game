# Avoid pushing directly to main

Pushing the `main` branch triggers an automatic Appwrite deployment. Development and review work must therefore remain on a feature branch until deployment is explicitly intended; agents must not push `main` as part of ordinary implementation work.
