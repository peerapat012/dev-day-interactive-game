# Allow one guest submission per round

Each guest may submit once per round, including while the host is reviewing a ready summary. Starting a new round resets the allowance; a ready summary alone does not close submissions, so late entries can be included by an explicit host refresh.
