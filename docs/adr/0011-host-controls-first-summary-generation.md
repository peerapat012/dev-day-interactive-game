# Let the host control first summary generation

Opening the Summary view with entries available starts the first summary generation. Guest submissions do not trigger generation directly, avoiding a one-entry snapshot while more entries are arriving and keeping generation under host control; once ready, Refresh summary remains explicit.
