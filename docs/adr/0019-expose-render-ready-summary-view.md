# Expose a render-ready summary view

The host summary workflow exposes lifecycle status, render-ready groups and summary cards, entry counts, pending entries, errors, action availability, and user intents. Room IDs, store setters, persistence details, operation tokens, and adapters remain behind the seam so the host UI depends on a small interface.
