# Only completed rounds enter history

Starting a new round requires a ready active summary; a round without a summary is not archived. This keeps saved-round history meaningful and avoids treating an explicit round transition as an accidental discard of unsummarized entries.
