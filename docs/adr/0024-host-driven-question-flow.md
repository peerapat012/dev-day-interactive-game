# Keep the quiz flow host-driven

The host drives every phase transition: lobby → question live → reveal → top-5 leaderboard → final podium. Guests never advance the game; they only join, answer the live question, and see feedback. This matches the existing host-led word cloud room and avoids races where guests could skip ahead of the host's pacing.