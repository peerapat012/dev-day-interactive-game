# Generate summaries from a captured entry snapshot

Each summary generation attempt captures the current round’s entries at its start. Entries submitted during generation belong to a later refresh, so a ready summary has a stable and reproducible input set rather than timing-dependent contents.
